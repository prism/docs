import Negation from '../../components/negation.tsx'

# Blocks Cause

Use the `bc:` parameter to list one or more blocks that caused an action.

- `bc:[block]`

Supports multiple (comma separated)!

`bc:lava` will query activities that caused actions.

Also see [btag](../btag).

## Negation

<Negation parameter='bc!:stone' note='Queries all blocks except "stone"' />