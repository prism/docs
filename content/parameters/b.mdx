# Blocks

Use the `b:` parameter to list one or more blocks.

- `b:[block]`

Supports multiple (comma seperated)!

`b:stone` will query activities that acted upon "stone" blocks.

Also see [btag](./btag).