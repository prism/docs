# ID

Use the `id:(activity id)` parameter to confine the search to activity id(s).

Supports multiple (comma seperated)!

An ID can be found by doing a lookup with the [nogroup](/flags/nogroup) flag. An ID is unique across the entire database, so it negates any other provided parameters.