import Command from '../../components/command.tsx'

# /pr wand

<Command command="wand (inspect|rollback|restore)" />

Toggles a wand of the specified type, or disables the current wand if no type provided.

To understand wands, please see [wands](/features/wands).