import Command from '../../components/command.tsx'

# /pr about

<Command command="about" note="(also shows by default if no commands used)" />

The about command shows version and author information.
