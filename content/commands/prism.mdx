# /pr

Everything in prism is a subcommand to ``/prism`` (or ``/pr`` for short).

The most important concept in Prism commands are [parameters](./parameters). Multiple commands accept parameters (lookup, rollback, restore, preview, and purge).