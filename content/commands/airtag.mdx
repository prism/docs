import Command from '../../components/command.tsx'

# /pr airtag

<Command command="airtag" />

Tags the held item with a unique six-character code, which allows prism to specially track activities for that item, including despawns and destructions.

Shows the existing airtag if the held item is already tagged.

See [AirTags](../concepts/airtags).