import Command from '../../components/command.tsx'

# /pr rollback

<Command command="rollback (parameters)" alias="rb" />

The rollback command will reverse any reversible changes based on your search parameters.

To understand rollbacks, see [rollbacks](/concepts/modifications#rollbacks).

To understand the parameters available and their use, please see [parameters](/parameters).
