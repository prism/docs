import Command from '../../components/command.tsx'

# /pr restore

<Command command="restore (parameters)" alias="rs" />

The restore command will re-apply any reversible changes based on your search parameters.

To understand restores, see [restores](/features/modifications#restores).

To understand the parameters available and their use, please see [parameters](/parameters).
