import Command from '../../components/command.tsx'

# /pr page

<Command command="page (page)" />

Normally, clicking "next" or "previous" in the UI will paginate through your results.

The page command gives you a manual way to paginate through your last paginated results.
