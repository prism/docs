# Physics

Use the `--physics true|false` (alias `-ph`) flag to enable physics during modifications.

By default, physics is disabled during rollbacks and restores to prevent unintended side effects and bukkit events.