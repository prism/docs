# No Group

Use the `--nogroup` (alias `-ng`) flag to view records individually, rather than grouped.

This will show you data not available when grouped, like their activity IDs, exact coordinates, etc.